[build]
  command = "npm install @rolldown/binding-linux-x64-gnu --no-save && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
